import React from 'react';
import Calculator from '../components/calculator';

const CalculatorPage = () => (
  <div className="calculatorPage">
    <h2>Let&prime;s do some math</h2>
    <div><Calculator /></div>
  </div>
);

export default CalculatorPage;
